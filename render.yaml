services:
  - type: web
    name: auto-devops-commander
    env: docker
    plan: free
    envVars:
      - key: PORT
        value: 3000
      - key: GEMINI_API_KEY
        sync: false
      - key: GITLAB_TOKEN
        sync: false
      - key: GITLAB_WEBHOOK_SECRET
        generateValue: true
